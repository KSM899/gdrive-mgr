# KDE Store submission

URL: https://store.kde.org/ → Add product → category **System Software**.

## Fields

**Title:** `gdrive-mgr — Google Drive for KDE Plasma`

**Subtitle / short description:**
> A KDE-native Google Drive client with on-demand mount, per-file pinning, in-app sharing, and Dolphin overlay badges.

**Long description:** (paste into the rich-text editor)

> If you've used Google Drive Desktop on Windows or macOS and missed the same experience on KDE Plasma, this fills that gap. Built on **rclone**.
>
> **What it does**
>
> - Mounts your Drive on demand at `~/GoogleDrive/<account>/` — your whole Drive appears as a regular folder; files only download when you open them.
> - "Keep always on this device" on any folder or file: right-click in Dolphin → *Google Drive → Keep always on this device*. The item gets a real local copy and stays in two-way sync.
> - Reactive background sync via inotify — local edits propagate to Drive within seconds, not minutes.
> - Dolphin overlay badges on every file/folder so you can tell at a glance what's synced, syncing, online-only, or in error.
> - Native Qt share dialog for adding people by email, setting Viewer / Commenter / Editor roles, and revoking access. No browser hop.
> - Multiple Google accounts side by side.
> - Bisync conflict resolution UI.
> - Disk-usage panel showing real on-disk footprint per account.
>
> Source, license (GPL-3.0-or-later), and install instructions:
> https://github.com/KSM899/gdrive-mgr

**Source URL:** `https://github.com/KSM899/gdrive-mgr`

**License:** `GPL-3.0+`

**Category:** *System Software → File Management* (or *Utilities*)

**Tags:** `google drive`, `rclone`, `cloud`, `sync`, `dolphin`, `plasma`, `kde6`, `fuse`

**Screenshots:** upload 3–5 from `docs/screenshots/` once captured.

**Logo:** the `folder-cloud` icon at 256×256 PNG would do; or render the Drive-style triangle yourself. Optional but a logo helps.
